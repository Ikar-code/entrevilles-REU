RewriteEngine On

# Si le fichier ou dossier demandé existe vraiment (CSS, images...), on le sert tel quel
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Sinon, tout passe par index.php qui fait le routage
RewriteRule ^ index.php [L]
